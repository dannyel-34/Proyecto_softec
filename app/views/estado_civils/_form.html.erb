<%= simple_form_for(@estado_civil, :html => { :class => 'form-horizontal', remote: true } ) do |f| %>

  <% if @estado_civil.errors.any? %>
	<div id="error_explanation">
  	<h2><%= pluralize(@estado_civil.errors.count, "error") %> prohibited this municipio from being saved:</h2>

  	<ul>
  	<% @estado_civil.errors.full_messages.each do |message| %>
    	<li><%= message %></li>
  	<% end %>
  	</ul>
	</div>
  <% end %>

  <div class="form-group">
   	<label for="name" class="col-sm-3 control-label">Estado</label>
   	<div class="col-md-4">
      	<%= f.input_field :nombre, class: 'form-control', size: "33" %>
   	</div>
  </div>

  <div class="form-group">
	<div class="col-sm-offset-2 col-sm-10">
  	<%= f.submit "Guardar", class: "btn btn-sm btn-success" %>
	</div>
  </div>

<% end %>
